# Optional: enables self-healing selectors via Gemini
# Without this, broken selectors fail with a clear error message
# With this, broken selectors auto-fix by sending page HTML to Gemini
GEMINI_API_KEY=
