import React from 'react';

const SwapButton = () => {
  return (
    <button
      className="swap-btn"
      disabled
      title="Swap requests coming soon"
      aria-label="Offer swap — coming soon"
    >
      Offer swap
    </button>
  );
};

export default SwapButton;
